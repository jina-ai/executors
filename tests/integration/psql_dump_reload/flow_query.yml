jtype: Flow
version: '1'
with:
  return_results: true
executors:
  - name: indexer_query
    uses: FaissPostgresSearcher
    metas:
      name: compound_query_indexer
    shards: $SHARDS
    replicas: 2
    polling: all
    uses_after: $USES_AFTER
